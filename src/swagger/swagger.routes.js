// Swagger UI is mounted directly in src/app.js for now.

/**
 * Placeholder route module (Swagger UI is mounted in `src/app.js`).
 *
 * @returns {undefined}
 */
export default function swaggerRoutes() {
  return undefined;
}


